extends kimchi/main
include mixins/videoBlock

block content
    .search__header
        h3 Search by #{searchingBy}
    .serarch__videos
        each item in videos
            +videoBlock({
                title: item.title,
                views: item.views,
                videoFile: item.videoFile
            })